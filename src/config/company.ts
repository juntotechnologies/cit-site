import metadata from "../../data/metadata.json";

/** Single source of truth for all CHEM-IS-TRY company contact info and branding constants. */
export const COMPANY = metadata.company;
export const TEAM = metadata.team;
